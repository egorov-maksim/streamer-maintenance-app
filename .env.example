# Server
PORT=3000
DB_FILE=./backend/streamer.db

# CORS (comma-separated origins)
ALLOWED_ORIGINS=http://localhost:3000

# Authentication: USERNAME:PASSWORD:ROLE per user, comma-separated
# Roles: superuser, admin, viewer
AUTH_USERS=admin:yourpassword:admin,viewer:yourpassword:viewer
